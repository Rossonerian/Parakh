# ML-03 worker handoff — reporting and Promptfoo boundary

## Task and ownership

- Task: ML-03, reporting/charts and Promptfoo export/import/quarantine boundary.
- Base observed at start: `f7ec930`.
- Exclusive files changed: `model_lab/reporting.py`, `model_lab/promptfoo.py`, `tests/test_reporting_promptfoo.py`, and this handoff.
- Shared contracts, configuration, CLI, storage, providers, execution, grading, analysis, routing, benchmark files, and `Memory.md` were not edited.
- Effective runtime model metadata: not exposed by this runtime; no model identity is claimed.

## Implemented

- Deterministic report object with metadata, provenance, per-model/domain/workflow/complexity/split summaries, sample counts, unknown-metric counts, limitations, and stable row ordering.
- JSON, CSV, Markdown, and standalone escaped HTML renderers.
- CSV formula-injection protection for cells beginning with `=`, `+`, `-`, or `@` after whitespace.
- Static SVG charts generated with the standard library. PNG charts use optional Matplotlib only when installed; otherwise `charts.json` records `PNG chart skipped: ...` and no fake PNG is written.
- Promptfoo manifest export from existing candidate projections only. Manifest includes version, run ID, prompt hashes, candidate messages/limits, and a deterministic manifest hash; protected evaluation/split metadata is structurally rejected.
- Versioned fixture import with manifest/hash/case/prompt-hash checks, duplicate and incomplete-result detection, deterministic normalized attempt records, source hash provenance, and quarantine files for invalid artifacts.
- Fail-closed invocation boundary: no subprocess or implicit live invocation; a caller must supply both `approved_plan=True` and an injected runner.

## Exact validation

From `/home/rosso/Projects/Parakh`:

```text
python3 -m compileall -q model_lab tests
```

Result: exit 0.

`pytest` was not installed (`/usr/bin/python3: No module named pytest`), so the focused test functions were run with a minimal stdlib `pytest.raises` compatibility harness. Exact result:

```text
test_csv_formula_cells_are_escaped_and_html_is_safe PASS
test_promptfoo_fixture_import_accepts_and_quarantines_tampering PASS
test_promptfoo_import_rejects_duplicate_and_unknown_case_ids PASS
test_promptfoo_live_invocation_fails_closed PASS
test_promptfoo_manifest_is_candidate_only_and_hashed PASS
test_report_bundle_writes_json_markdown_csv_html_and_charts PASS
test_report_formats_are_deterministic_and_preserve_unknowns PASS
```

Benchmark validation was also run:

```text
python3 - <<'PY'
from model_lab.benchmark import load_suite, validate_suite
s = load_suite('benchmarks/seed_cases.jsonl')
assert validate_suite(s)['cases'] == 60
print('seed validation: PASS (60 cases)')
PY
```

Result: `seed validation: PASS (60 cases)`.

The same stdlib exercise covered candidate-only export, accepted fixture import, tampered prompt-hash quarantine, report bundle generation, HTML escaping, CSV formula protection, SVG generation, optional PNG behavior, and fail-closed live invocation. No network, provider, paid call, or live Promptfoo process was used.

## Commit and recovery

The worker commit containing only the four owned files is recorded in the final response. To recover the candidate, cherry-pick that commit onto the Boss integration branch after independent Supervisor review. Worker code is not self-approved.

## Remaining gaps and risks

- Full pytest execution is pending installation of the project dev dependency in the integration environment.
- This worker does not wire reports into the CLI or storage/execution pipeline because those files are outside the assigned ownership.
- PNG output depends on an explicitly optional Matplotlib installation; SVG remains available without it.
- Promptfoo live execution is intentionally not implemented here; the approved-plan/runner boundary must be connected by the owning integration code with budget and provider authorization checks.
