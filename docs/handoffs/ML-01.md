# ML-01 worker handoff — storage, providers, execution, ingestion

## Task and baseline

- Task: ML-01, worker-storage-execution.
- Requested role/model: Luna worker 1 / `gpt-5.6-luna` / medium.
- Effective runtime model metadata: not exposed by this runtime; no claim is made beyond the requested role.
- Repository HEAD at intake: `f7ec930` (`Define disjoint ModelLab worker task cards`). The task card listed `00fb5e3` as its base; current checkout already included the task-card commit.

## Files changed

- `model_lab/storage.py`
- `model_lab/providers/__init__.py`
- `model_lab/providers/base.py`
- `model_lab/providers/fake.py`
- `model_lab/providers/live.py`
- `model_lab/execution.py`
- `model_lab/ingestion.py`
- `model_lab/budget.py`
- `tests/test_storage_execution.py`
- `tests/test_ingestion_budget.py`
- this handoff

No schemas, benchmark, isolation, CLI, configuration, lockfile, or `Memory.md` files were edited.

## Behavior delivered

- SQLite schema for runs, immutable attempts/raw records, grades, reviews, provenance, budget reservations, quarantines, and indexes.
- Duplicate attempt/grade/review/provenance protection and append-only attempt API.
- Provider protocol and response/capability contracts.
- Deterministic fake variants: correct, incorrect, incomplete, malformed, failure, missing metadata, and adversarial output.
- Fail-closed Ollama/OpenRouter adapter stubs; no live request implementation or secret persistence.
- Bounded execution over full or selected case sets, retries for provider/timeout failures, cancellation, partial status, failure classification, provenance/prompt hashes, and durable attempts.
- JSONL, JSON, and CSV import with metadata validation, duplicate/mismatch quarantine, and unknown usage preserved as `null`.
- Atomic request/cost reservations and settlement with unknown actual cost retained as unknown. Request limits count settled and active reservations; explicitly released reservations are reusable.

## Validation evidence

Environment: `/home/rosso/Projects/Parakh`, Python 3.12-compatible standard-library implementation. `pytest` is not installed in the environment.

Commands and results:

```text
python3 -m compileall -q model_lab tests
exit 0

pytest -q tests/test_storage_execution.py tests/test_ingestion_budget.py
command not found: pytest

python3 -m pytest -q tests/test_storage_execution.py tests/test_ingestion_budget.py
No module named pytest

python3 -m compileall -q model_lab tests && python3 <focused stdlib assertions>
manual focused checks: PASS
```

The focused assertions exercised: two-case execution and run status, immutable duplicate rejection, retry preservation, request-budget stopping at one attempt with `partial` status, duplicate/mismatched import quarantine, and fail-closed live provider behavior. The exact request-budget regression scenario now yields one persisted attempt and `partial` status.

## Known gaps / risks

- Pytest could not run because the environment lacks the declared optional dev dependency; installation was not performed by this worker.
- Live Ollama/OpenRouter calls are intentionally fail-closed stubs pending an approved official-client integration.
- The worker did not implement grading, comparison, reporting, routing, CLI, or Promptfoo integration; those remain with their assigned owners/integration work.
- Storage currently exposes immutable writes through its API, but direct SQL users with filesystem access could update rows; SQLite file permissions/operational hardening remain integration concerns.
- Runtime/case-count limits are represented in schemas; execution enforces request, cost, cancellation, and runtime stopping, while full report accounting belongs to integration.

## Recovery and next action

Candidate commit is the commit containing this handoff. Recover with `git show <candidate-commit>` or cherry-pick it into the Boss integration branch. Boss should review the exact candidate, run the full pytest suite after installing the documented dev dependency, and integrate serially with the other workers’ contracts.
