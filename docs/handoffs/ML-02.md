# ML-02 handoff — grading, review, comparison, regression, routing

Task ID and requirement IDs: ML-02; deterministic grading, blind review, matched comparison/regression, routing evidence.

Role and effective model evidence: requested Worker / `gpt-5.6-luna` / medium. The runtime exposed no effective model metadata, so effective model is **unknown**; no identity is inferred from the role label.

Base commit and candidate commit/tree: requested base `f7ec9302b0a2d61c2e5eec1d580d8d197970c09a`; task card records `00fb5e3`, which conflicts with the requested/current base. Candidate commit is recorded below.

Files changed:

- `model_lab/grading.py`
- `model_lab/analysis.py`
- `model_lab/review.py`
- `model_lab/routing.py`
- `tests/test_grading_analysis.py`
- `tests/test_review_routing.py`
- `docs/handoffs/ML-02.md`

Behavior changed and why:

- Added exact text (boundary-whitespace tolerant), normalized text, exact JSON, safe JSON-schema, arithmetic, and required-field graders.
- Non-success attempts, malformed JSON, and semantic rubric cases explicitly abstain with provenance instead of becoming zero scores. Rubric abstentions retain `method="rubric"`.
- Added deterministic grade IDs and evidence describing grader type, inputs, case, model, and evaluation method.
- Added matched-case comparison grouped by domain, workflow/family, complexity, and split. Labels are inferred from grade evidence; left-minus-right deltas are descriptive only and family is the independence unit.
- Added regression deltas that preserve null/insufficient values and never claim statistical significance.
- Added blind review export/import with deterministic candidate labels and hashed run aliases; provider/model/latency/cost/ranking metadata do not cross the review boundary.
- Added draft-only routing recommendations with evidence IDs, coverage, eligibility, fallbacks, synthetic labeling, and explicit limitations. No production configuration is modified.

Commands run, exit status, environment:

- `python3 -m compileall -q model_lab tests` — 0.
- Focused stdlib verification script covering the 60-case suite load, exact/JSON/arithmetic grading, matched labels/delta, router candidate attribution, blind run-ID redaction, and synthetic evidence — 0 (`focused correction checks: PASS`).
- `git diff --check` — 0.
- `pytest -q ...` and `python3 -m unittest discover ...` could not execute the pytest tests because this environment has no `pytest` module/command. The unittest discovery also failed at import for the same missing optional dev dependency; no test failure was attributed to ML-02 behavior.

Manual scenarios and actual observations: A valid exact-JSON candidate scored 1.0; a valid wrong JSON candidate scored 0.0; malformed JSON abstained; non-success attempts abstained; rubric grading abstained with `method="rubric"`; model labels inferred as `good`/`weak`; router selected `good`; blind export contained neither `secret-model` nor `run-secret-model` and used `blind-run-<hash>`.

Commit/patch location and recovery procedure: candidate commit is this handoff commit. Recover with `git show <candidate-commit> -- model_lab/grading.py model_lab/analysis.py model_lab/review.py model_lab/routing.py tests/test_grading_analysis.py tests/test_review_routing.py docs/handoffs/ML-02.md` and cherry-pick only that commit into the Boss integration branch after independent review.

Known failures, skipped checks, risks:

- Pytest is unavailable until the repository dev extra is installed; no dependency/config files were changed by this worker.
- This worker did not modify or validate the other workers' storage/execution/reporting contracts. Boss integration must run the full suite and resolve any API mismatches.
- Deterministic graders do not establish semantic correctness; rubric review/judge calibration remains required.
- Statistical significance, cost, and latency remain null/unsupported in routing drafts unless measured evidence is supplied.

Next action: Boss should independently review this exact commit, integrate it serially with storage/execution/reporting contracts, install the documented dev dependencies, and run the full 60-case offline pipeline plus pytest.
