# Daily AI Agent product-source imports

This directory is the versioned intake area for the actual Daily AI Agent PRD, Architecture, Tier Entitlements, and Design documents.

`SOURCE_MANIFEST.json` records the exact source paths, revisions, and hashes. At this continuation point, only the root `Architecture.md` exists in the available workspace; the actual PRD, Design, and Tier Entitlements documents were not found. No substitute content is fabricated.

Before authorizing a paid pilot, place the operator-provided source documents here, preserve their original filenames/revisions, calculate SHA-256 hashes, and update the manifest in a separate reviewable commit. Router constraints must then be mapped against those imported sources.

